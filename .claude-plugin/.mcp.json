{
  "mcpServers": {
    "smartify": {
      "type": "http",
      "url": "${user_config.api_base}/mcp/${user_config.hivemind_id}",
      "headers": {
        "Authorization": "Bearer ${user_config.api_key}"
      }
    }
  }
}
